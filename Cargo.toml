[package]
name = "krkrz_plugin_base"
version = "0.1.0"
edition = "2024"

[dependencies]
windows = { version = "0.62", features = ["Win32_System_Com", "docs"], optional = true }

[features]
default = ["com"]
com = ["windows"]

[build-dependencies]
bindgen = "0.72"
cc = "1.2"
